import { breakpoints } from './breakpoints';

const container = {
  maxWidth: breakpoints.values.md,
};

const header = {
  height: 60,
  zIndex: 1000,
};

const gallery = {
  height: 500,
  smHeight: 250,
};

export const components = {
  container,
  header,
  gallery,
};
